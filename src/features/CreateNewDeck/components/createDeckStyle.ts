import { StyleSheet } from 'react-native';

const createDeckStyle = StyleSheet.create({
	container: {
		backgroundColor: 'steelblue',
		width: '100%',
		height: '100%',
		padding: 10,
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	},
});

export default createDeckStyle;
